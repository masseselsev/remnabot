# === Core Settings ===
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=12345678,87654321
ADMIN_GROUP_ID=-1001234567890

# === Bot Identity ===
# Used for redirects back to bot after payment
BOT_LINK=https://t.me/YourBotName

# === Remnawave Panel ===
REMNAWAVE_URL=https://panel.example.com
REMNAWAVE_API_KEY=your_panel_api_key

# === Database ===
POSTGRES_USER=remnabot
POSTGRES_PASSWORD=remnabot
POSTGRES_DB=remnabot
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# === Payments ===

# 1. YooKassa (Primary - Bank Cards RF)
# Cabinet: https://yookassa.ru/my/merchant/integration/api-keys
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# 2. Telegram Stars (Optional)
PAYMENT_PROVIDER_TOKEN_STARS=

# 3. Future / High-Risk Providers
PAYMENT_PROVIDER_TOKEN_PLATEGA=
PAYMENT_PROVIDER_TOKEN_TRIBUTE=

# === Webhook Configuration ===
# Required for YooKassa callbacks. Not needed for Polling if payment check is manual (but we use auto).
# === Webhook Configuration ===
# Logic: If WEBHOOK_URL is set -> Bot starts in Webhook mode via aiohttp (port 8000 inside container).
# You must configure your own Reverse Proxy (Nginx/Caddy/Traefik) to forward requests to port 8000.
# WEBHOOK_URL=https://your-domain.com/webhook
# WEBHOOK_HOST=0.0.0.0
# WEBHOOK_PORT=8000
